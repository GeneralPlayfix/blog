<nav class="navbar navbar-expand-lg navbar-dark bg-dark">
    <div class="container-fluid">
        <a class="navbar-brand" href="{{ path("home") }}" class="menu__link r-link text-underlined">Mon blog</a>
        <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
        </button>
        <div class="collapse navbar-collapse" id="navbarNav">
            <ul class="navbar-nav">
                <li class="nav-item">
                    <a class="nav-link" aria-current="page" href="{{ path("home") }}" class="menu__link r-link text-underlined">Accueil</a>
                </li>
                <li class="nav-item">
                    <a class="nav-link" href="{{ path("blog") }}" class="menu__link r-link text-underlined">Blog</a>
                </li>
                <li class="nav-item">
                    <a class="nav-link" href="{{ path("contactWithoutNothing") }}" class="menu__link r-link text-underlined">Contact</a>
                </li>
                <li class="nav-item">
                    <a class="nav-link" href="{{ path("create_article") }}" class="menu__link r-link text-underlined">Créer un article</a>
                </li>
            </ul>
        </div>
    </div>
</nav>